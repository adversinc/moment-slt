{
  "name": "@advers/moment-SLT",
  "version": "4.0.1",
  "description": "SecondLife timezone manipulation",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "prepare": "npm run build",
    "test": "npm run build && ts-mocha test/**/*.t.ts",
    "test:watch": "npm run build && ts-mocha test/**/*.t.ts -w --watch-files '**/*.ts'"
  },
  "publishConfig": {
    "registry": "https://npm.metabizlist.com"
  },
  "repository": "https://github.com/adversinc/moment-slt.git",
  "author": "Dino Fabrello <dinofabrello@slbiz2life.com>",
  "license": "MIT",
  "devDependencies": {
    "@types/expect": "^24.3.0",
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.11.20",
    "dayjs": "^1.11.19",
    "mocha": "^9.1.2",
    "mocha-logger": "^1.0.7",
    "nyx": "^0.1.0",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.3.3"
  },
  "peerDependencies": {
    "dayjs": "^1.11.10"
  }
}
